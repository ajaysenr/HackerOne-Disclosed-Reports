# Weak random number generator used in concrete/authentication/concrete/controller.php

| | |
|---|---|
| **Report ID** | [31171](https://hackerone.com/reports/31171) |
| **Program** | [Concrete CMS](../by-program/concretecms.md) |
| **Severity** | None |
| **Weakness** | [Cryptographic Issues - Generic](../by-weakness/cryptographic-issues---generic.md) |
| **Bounty** | — |
| **Votes** | 2 |
| **Reporter** | [voodookobra](https://hackerone.com/voodookobra) |
| **Asset** | — (—) |
| **CVE IDs** | — |
| **Reported** | 2014-10-12 |
| **Disclosed** | 2014-10-26 |
| **Resolution** | Resolved |

## Program Summary

> *No official summary provided by the program.*

## Vulnerability Details

```php

    private function genString($a = 20)
    {
        $o = '';
        $chars = 'abcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*()_+{}|":<>?\'\\';
        $l = strlen($chars);
        while ($a--) {
            $o .= substr($chars, rand(0, $l), 1);
        }
        return md5($o);
    }
```

Using substr(rand()) then running md5() on the output would be better replaced by using bin2hex() and either openssl_random_pseudo_bytes($a) or mcrypt_create_iv($a, MCRYPT_DEV_URANDOM)

For example:

```php
    private function genString($a = 20)
    {
        if (function_exists('mcrypt_create_iv')) {
            return bin2hex(mcrypt_create_iv($a, MCRYPT_DEV_URANDOM);
        }
        return bin2hex(openssl_random_pseudo_bytes($a));
    }
```
